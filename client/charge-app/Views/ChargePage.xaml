<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="charge_app.Views.ChargePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:charge_app.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"

    mc:Ignorable="d">

    <Grid>
        <StackPanel Padding="48,24,24,0">

            <TextBlock
                Style="{StaticResource DetailSubTitleStyle}"
                Text="充电类型" />

            <DropDownButton
                x:Name="DropDownButton"
                Content="请选择类型"
                Margin="0 16 0 0">
                <DropDownButton.Flyout >
                    <MenuFlyout Placement="Bottom" >
                        <MenuFlyoutItem Text="快充" Click="FastType"/>
                        <MenuFlyoutItem Text="慢充" Click="SlowType"/>
                    </MenuFlyout>
                </DropDownButton.Flyout>
            </DropDownButton>

            <TextBlock
                Margin="{StaticResource SmallTopMargin}"
                Style="{StaticResource DetailSubTitleStyle}"
                Text="充电请求额度" />
            <TextBox
                Margin="0 16 0 0"
                Width="300"
                HorizontalAlignment="Left"
                Text="{x:Bind ViewModel.Req.Amount, Mode=TwoWay}"
                />

        </StackPanel>

        <Button
            Margin="48,0,8,0"
            MinWidth="200"
            Content="提交"
            Click="Charge"
            Style="{StaticResource AccentButton}" />
    </Grid>
</Page>
