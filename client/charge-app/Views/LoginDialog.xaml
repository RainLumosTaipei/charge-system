<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="charge_app.Views.LoginDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:charge_app.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:charge_app.Converters"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    mc:Ignorable="d">

    <ContentDialog.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </ContentDialog.Resources>

    <StackPanel VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <TextBox PlaceholderText="用户名" Text="{Binding Username, Mode=TwoWay}"/>
        <PasswordBox PlaceholderText="密码" Password="{Binding Password, Mode=TwoWay}"/>
        <TextBlock
            Text="{Binding ErrorMessage}"
            Foreground="Red"
            Visibility="{Binding HasError, Converter={StaticResource BoolToVisibilityConverter}}" />
    </StackPanel>

</ContentDialog>
